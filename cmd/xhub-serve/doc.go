/* 
xhub-serve is a command for starting the xhub server.

Usage:
	xhub-serve [flags]

The flags are:
	-addr
		host name or ip address (`localhost:8081`)
	-dbfile
		name of the boltdb file for persisting xhub data (`xhub.db`)
*/
package main
